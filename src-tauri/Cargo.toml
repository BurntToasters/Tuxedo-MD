[package]
name = "tuxedomd"
version = "0.1.0-alpha.1"
description = "A sleek, local-first Markdown editor"
authors = ["BurntToasters"]
edition = "2021"
license = "MPL-2.0"
repository = "https://github.com/BurntToasters/Tuxedo-MD"

[lib]
name = "tuxedomd_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = { version = "1", features = ["pure"] }
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"
tauri-plugin-process = "2"
tauri-plugin-notification = "2"
thiserror = "2"
walkdir = "2"

[target.'cfg(any(target_os = "macos", target_os = "windows", target_os = "linux"))'.dependencies]
tauri-plugin-single-instance = "2"
tauri-plugin-updater = "2"

[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Storage_FileSystem"] }

[features]
default = []
# App Store builds pass --features mas (opaque window / store chrome). Tauri's
# allowlist requires macos-private-api on the dependency whenever conf enables it;
# App Store overlay sets macOSPrivateApi: false so private APIs are not used at runtime.
mas = []

[dev-dependencies]
minisign-verify = "0.2.5"

[profile.release]
codegen-units = 16
lto = "thin"
